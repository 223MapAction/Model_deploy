# Core requirements
fastapi==0.109.0
uvicorn[standard]==0.29.0
httpx==0.26.0
pydantic==2.5.3
requests==2.31.0
python-dotenv==0.19.2

# GEE dependencies - minimal
earthengine-api==1.1.3
pillow==10.2.0
numpy
matplotlib

# Database
redis
databases==0.9.0
asyncpg==0.29.0
psycopg2-binary

# LLM
openai==1.68.2

# The following dependencies are commented out to simplify the build
# They will be installed by pip if needed as dependencies of other packages
# scipy==1.14.0
# shapely==2.0.6
# rasterio==1.4.1
# geopandas==1.0.1
# azure-storage-blob
# azure-identity
# langchain==0.1.5
# langchain-community==0.0.18 